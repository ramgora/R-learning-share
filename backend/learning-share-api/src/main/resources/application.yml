spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/learning_share
    username: postgres
    password: rui19991102

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
